<Window x:Class="ProjectBike.Desktop.Views.LoadingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Height="300" Width="500">

    <Border Background="{DynamicResource MaterialDesign.Brush.PrimaryDark}" CornerRadius="16">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <materialDesign:PackIcon Kind="Bike" Width="80" Height="80" 
                                     HorizontalAlignment="Center" Foreground="White" 
                                     Margin="0,0,0,10"/>
            
            <TextBlock Text="RENT APP" FontSize="36" FontWeight="Bold" 
                       HorizontalAlignment="Center" Foreground="White"/>
            
            <TextBlock Text="Trwa uruchamianie systemu..." FontSize="16" 
                       HorizontalAlignment="Center" Foreground="White"
                       Opacity="0.8" Margin="0,10,0,30"/>

            <ProgressBar x:Name="DeterminateCircularProgress"
                         Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="50" Height="50"
                         Foreground="White" />
        </StackPanel>
    </Border>
</Window>